%p#notice= notice
%h1 Client list
= link_to 'New Client', new_client_path, class: 'btn btn-primary'
%table.table.table-striped
  %thead
    %tr
      %th Title
      %th{colspan: 3}
  %tbody
    - @clients.each do |client|
      %tr
        %td= link_to client.name, client_path(client.id)
        %td= link_to 'Edit', edit_client_path(client), class: 'btn btn-info'
        %td= link_to 'Destroy', client, method: :delete, data: {confirm: 'Are you sure?'}, class: 'btn btn-warning'

%br/
