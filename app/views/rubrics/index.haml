%p#notice= notice
%h1 Rubric list
= link_to 'New Rubric', new_rubric_path, class: 'btn btn-primary'
%table.table.table-striped
  %thead
    %tr
      %th Title
      %th{colspan: 3}
  %tbody
    - @rubrics.each do |rubric|
      %tr
        %td= link_to rubric.title, rubrics_path(rubric_id: rubric.id)
        %td= link_to 'Edit', edit_rubric_path(rubric), class: 'btn btn-info'
        %td= link_to 'Destroy', rubric, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-warning'

%br/

